# ─────────────────────────────────────────────────────────────────────────
# 이 프로젝트가 검증된 버전 (2026-07 기준, 이 조합에서 전체 테스트 통과)
#
# ★ LangGraph 는 interrupt()/Command API 가 버전마다 달라 민감합니다.
#   사내에서 쓰는 실제 버전을 알려주시면 그 버전에 맞춰 조정하겠습니다.
#   코드는 버전 관용적으로 작성돼 있습니다:
#     - 인터럽트 감지: StateSnapshot.interrupts 우선, 없으면 tasks[].interrupts 폴백
#     - 체크포인터: InMemorySaver(구 MemorySaver) — 이름만 바꾸면 구버전 호환
#     - astream_events(version="v2")
# ─────────────────────────────────────────────────────────────────────────

# 그래프 / LLM
langgraph==1.1.2
langgraph-checkpoint==4.1.1
langgraph-prebuilt==1.0.13
langchain-core==1.4.9
langchain-openai==1.1.8        # 사내 OpenAI 호환 endpoint 접속용
pydantic==2.12.5

# API 서버
fastapi==0.140.0
uvicorn==0.51.0

# 프론트 / 클라이언트
streamlit==1.60.0
httpx==0.28.1                  # SSE 스트리밍 클라이언트

# 설정
python-dotenv==1.2.2

# 노트북에서 데모할 때만 필요 (선택)
# ipykernel
# nbclient
