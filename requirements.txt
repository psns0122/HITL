# =========================================================================
# 사내 확인 버전에 맞춤 (2026-07 확인)
#   langgraph 1.1.2 / langchain-core 1.4.9 / langchain-openai 1.1.8 / pydantic 2.12.5
# 이 조합에서 전체 테스트(시나리오 12종 · SSE 8종 · UI 5종 · 노트북 42셀) 통과 확인.
# =========================================================================

# ── 그래프 / LLM ─────────────────────────────────────────────────────────
# ★ 정확히 고정합니다. LangGraph 는 interrupt()/Command/StateSnapshot.interrupts
#   API 가 버전마다 달라, 올리면 HITL 재개 동작이 깨질 수 있습니다.
#   (langgraph-checkpoint 4.1.1 / langgraph-prebuilt 1.0.13 이 함께 설치되는 것을 확인)
langgraph==1.1.2
langchain-core==1.4.9
pydantic==2.12.5

# 사내 LLM 게이트웨이(OpenAI 호환) 호출용
#   pptx-vision-rag 는 langchain-openai>=0.2.0 로 두지만, 여기서는 langchain-core 1.4.9
#   와 짝이 맞는 버전을 고정합니다.
langchain-openai==1.1.8

# ── API 서버 ─────────────────────────────────────────────────────────────
# 표준 API 만 쓰므로 하한만 둡니다 (검증: fastapi 0.140.0 / uvicorn 0.51.0)
fastapi>=0.110.0
uvicorn>=0.27.0

# ── 프론트 / 클라이언트 ───────────────────────────────────────────────────
# 하한은 사내 pptx-vision-rag 와 동일하게 맞춤 (검증: streamlit 1.60.0 / httpx 0.28.1)
#   streamlit: st.status / st.chat_input / streamlit.testing.v1.AppTest 사용 (1.28+ 필요)
streamlit>=1.30.0
httpx>=0.27.0

# ── 설정 ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── 데모 노트북 실행용 (선택) ─────────────────────────────────────────────
# notebooks/hitl_demo.ipynb 를 돌릴 때만 필요합니다.
# ipykernel>=6.29.0
# nbclient>=0.10.0
